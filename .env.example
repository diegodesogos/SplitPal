# Application Settings
NODE_ENV=development
BACKEND_PORT=5001
VITE_PORT=3001

# JWT Authentication (Required)
# Generate using: openssl rand -base64 32
JWT_SECRET=your-generated-jwt-secret

# Google OAuth2 Configuration (Required for authentication)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# URLs and Endpoints
# For local development, use localhost URLs
# For production, set to your deployed domain (e.g., https://api.splitpal.com)
BACKEND_URL=http://localhost:5001
VITE_API_URL=http://localhost:5001
FRONTEND_URL=http://localhost:3001

# Production Settings (update these values in Vercel environment variables)
# NODE_ENV=production
# BACKEND_URL=https://api.splitpal.com
# VITE_API_URL=https://api.splitpal.com
# FRONTEND_URL=https://splitpal.com

# Storage Configuration
STORAGE_TYPE=database
DATABASE_URL=your-database-connection-string
